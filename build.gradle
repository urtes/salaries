plugins {
    id 'java'
}

jar {
    manifest {
        attributes(
                'Main-Class': 'lt.interviewtasks.salaries.SalariesApplication'
        )
    }
}

version '2.0'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
